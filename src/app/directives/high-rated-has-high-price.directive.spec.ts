import { HighRatedHasHighPriceDirective } from './high-rated-has-high-price.directive';

describe('HighRatedHasHighPriceDirective', () => {
  it('should create an instance', () => {
    const directive = new HighRatedHasHighPriceDirective();
    expect(directive).toBeTruthy();
  });
});
